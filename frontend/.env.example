# Public URL of the FastAPI app (no trailing slash).
# Local against local API: leave this empty (Vite proxies /api → http://127.0.0.1:8000)
# Local UI against Vercel API, or Vercel UI build:
VITE_API_URL=https://YOUR-FASTAPI-PROJECT.vercel.app
